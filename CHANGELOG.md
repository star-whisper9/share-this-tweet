# Changelog

## [Unreleased]

### Added

- 复用 X 响应内的自动译文，卡片按 Grok 译文／X 原文结构展示，支持一层引用；译文异常时在界面警告并回退原文，不额外调用翻译接口。
- 提供源语言、译文语言、原文及译文模板变量（含引用变量）、可选双语复制预设和翻译场景试算。
- X／Grok 单色徽标按明暗主题着色，保留透明轮廓，不改变用户头像与图片。

- 小图画框最低输出宽度为 640 像素，按比例提高文字绘制分辨率；大图保持原尺寸，原图下载不变。

- 来源画框与推文卡片共用自动明暗主题和配色，设置页预览同步适配；同一批次导出保持主题一致，不增加主题选择入口。

- 扩展推文、作者、媒体和引用元信息变量，统一变量注册表、UTC 日期格式、缺值默认及可选区块；设置页提供完整指南、搜索、快捷插入和场景试算。
- 调整命名与复制预设，保留用户已有模板；来源画框模板支持多行输入和预览。
- 常规及引用卡片在整张图底部统一显示小字号、低对比度来源脚注，引用来源依次编号，扩展标识仅出现一次。
- 来源画框和设置页模拟预览使用纯数字 ID 与低对比度扩展标识。

- 支持一层引用推文：区分双方作者、正文、来源和媒体操作；复制文本及推文卡片包含引用内容，未知或不可用引用保留明确状态。
- 来源记录保存引用快照并兼容旧归档；引用媒体按所属推文命名和记录。

## [0.2.1] - 2026-09-08

### Removed

- 移除原生文本／图片分享入口和适配代码；所有客户端统一提供复制推文文字、保存推文卡片。保留历史分享记录的查询与导入兼容。

### Known Issues

- 原生分享暂不实现，也未排定后续版本：Firefox 尚未支持 Web Share 文件分享（[Mozilla 1635700](https://bugzilla.mozilla.org/show_bug.cgi?id=1635700)），Android 文本分享存在正文丢失问题（[Mozilla 1896224](https://bugzilla.mozilla.org/show_bug.cgi?id=1896224)）。用户可复制后粘贴，或保存图片后手动发送。调查日期：2026-09-08。

### Changed

- 收紧导出弹层留白，按所选媒体和整条推文分组，简化按钮说明并保留默认方向标记。
- 优化设置页窄屏布局，修复分类 Tab 溢出，预留滚动条空间以避免切换面板时横向位移。
- 画框保持原图分辨率，不透明图片输出 JPEG，含透明像素的图片输出 WebP，编码质量 0.92。
- 卡片收紧边距、行距和页脚，多图按宽高比排列并完整保留内容；继续采用 2 倍渲染。
- 画框与卡片共享会话内有界图片缓存和请求队列；卡片保存合并重复生成任务，按内容、主题和文件名失效。
- 路由退出中止图片请求，生成结束释放图像及画布资源。
- 将页面路由协调、弹层交互、动作展示、媒体选择和导出会话分离，统一单项与批量导出的状态及来源记录处理。
- 通过推文会话生命周期隔离过期任务，取消、异常及批量完成均不会更新已离开的页面。
- 自动化测试改为通过公开会话接口验证必要行为，移除易变错误文案和默认展示字符串断言；类型检查启用未使用变量与参数检查。

### Added

- 支持纯文字推文卡片 PNG，自动折行并根据正文调整高度，保留作者、时间与来源信息。
- 所有客户端均提供推文卡片保存入口；没有照片的视频或 GIF 推文也可保存文字与来源卡片。
- 无照片卡片的文件名模板 `{media.index}` 使用 `0`；输出记录不包含媒体序号，仍可通过文件名反查来源。使用 `{media.type}` 的自定义模板会明确提示缺少媒体上下文。

### Fixed

- 保留卡片文件名的 `_card` 标记，避免模板扩展名使标记被移除；生成期间切换推文时不再输出过期卡片。
- 在文档开始加载时安装推文响应拦截器，DOM 就绪后启动界面，避免等待整页加载而错过早期推文请求。
- 兼容 XHR 文本及已解析的 JSON 响应；避免重复安装拦截器或为复用的 XHR 重复注册监听。
- 区分读取器启动失败与目标推文等待超时，提供显式重新加载入口；晚到数据仍可恢复界面。
- 明确记录响应解析失败，并保持页面原有 fetch 请求结果与错误行为。

## [0.2.0] - 2026-09-07

### Added

- 新增 IndexedDB 本地来源记录和输出记录。
- 新增设置页“来源记录”管理界面，支持搜索、详情查看、单条删除、全部清空以及 JSON 导入导出。
- 新增 1～4 张照片的推文卡片生成和 PNG 保存。
- 推文卡片支持根据 X 页面主题或系统主题自动选择亮色/暗色配色，并使用高分辨率 Canvas 渲染文字。
- Android Firefox 新增独立的分享文本、分享图像和复制文本操作。

### Changed

- 推文卡片自动使用当前推文的全部照片，不受媒体选择状态影响；视频和 GIF 不嵌入卡片。
- 桌面平台不显示原生分享按钮，Android 分享能力单独检测。
- 分享图像失败时保留已生成的卡片，并在错误提示中提供显式保存操作；不会自动切换到复制或保存。
- 继续保留原图、画框、复制文本和来源记录之间的独立输出记录。

### Known Issues

- Firefox Android 当前可能不支持通过 Web Share API 分享生成的图片文件；此时原生分享按钮不会显示，用户可以保存卡片后从系统相册分享。
- 即使能力检测通过，部分设备或 Firefox 版本仍可能在实际图片分享时失败；失败后可保存已经生成的卡片并再次重试分享。

### Notes

- `v0.2.0` 在 `v0.1.1` 基础上加入本地来源管理、推文卡片和 Android 原生分享能力。
- macOS Firefox 已完成主要功能验证；Android Firefox 图片原生分享仍处于兼容性观察阶段。

## [0.1.1] - 2026-09-07

### Added

- 重构分享弹层和设置页，支持浏览器工具栏弹出卡片进入设置。
- 支持照片上方或下方来源画框，并根据图像宽度自动使用单栏或双栏排版。
- 支持 `{author.avatar}` 画框模板变量，头像获取失败时回退到内置 X logo。
- 支持媒体多选，以及原图和画框操作的批量保存。

### Changed

- 统一原图按钮和上下画框按钮的单选、批量操作逻辑。
- 批量画框时，照片添加画框，视频和 GIF 保持原样保存。
- 没有选中媒体时，媒体操作按钮保持禁用状态。
- 简化设置页视觉元素，保留模板校验、预览和保存流程。
- 增加 Prettier 格式检查配置。

### Fixed

- 优化画框字体、边框、颜色和上下留白。
- 修复头像参与画框文字宽度计算，以及画框操作按钮尺寸和间距问题。
- 修复批量操作状态、失败提示和重试逻辑。

### Notes

- `v0.1.1` 包含自 `v0.1.0` 发布提交 `19308db6e31865e75bff025f3e96e50b1e330c2b` 以来的功能增强。
- macOS Firefox 已完成主要功能验证；Android Firefox 实机验证仍待后续进行。

## [0.1.0] - 2026-09-07

### Added

- X 推文详情页扩展入口和 Bottom sheet 操作界面。
- Tweet 数据拦截、标准化和不完整响应合并。
- 照片原图、普通视频和 GIF MP4 下载。
- 文件名模板、文件名清理和媒体扩展名处理。
- 推文文本模板和剪贴板复制。
- 照片底部单栏/双栏来源画框 PNG。
- 设置页及文件名、画框、复制文本模板的保存、校验和预览。
- macOS 与 Firefox Android 的下载路径适配。

### Fixed

- 防止后续不完整推文响应覆盖作者等完整元信息。
- 移除媒体实体对应的 `t.co` 短链接，同时保留正文中的普通链接。
- 允许保存、复制和生成画框操作在完成后再次执行。

### Known Issues

- 有时会出现推文已完成加载但无法获取推文信息的情况，刷新后有概率正常，待排查

### Notes

- `v0.1.0` 功能实现完成。
- macOS Firefox 已完成主要功能验证；Android 后续实机测试中的兼容性问题进入 `v0.1.1`。
- 项目独立开发，使用 GPL v3；参考 `twitter-improvements` 的部分源码和实现思路。
