# 项目验证规范

- 只对必要行为和高风险边界编写自动化测试，优先复用现有验证，减少冗余测试。
- 禁止用易变 UI 文案、错误提示、默认展示字符串或整段界面快照的比较／断言验证功能。
- 模板解析等功能可验证明确输入对应的确定性输出数据；不把产品措辞当作功能契约。
- 测试通过公开模块接口验证行为，不通过强转访问控制器私有字段或方法。
- 常规实机验证由星语负责，代理不主动使用 CUA 测试。只有修复调测等需要复杂调试，或星语明确要求时，代理才使用 CUA。
- 代理完成必要测试、类型检查与构建，提供简短的人工回归场景，并如实说明未进行的实机验证。
- 必要验证通过后收尾；只有新改动、失败或具体风险才扩大检查。
