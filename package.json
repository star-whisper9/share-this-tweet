{
  "name": "share-this-tweet",
  "version": "0.3.0",
  "private": true,
  "description": "A Firefox extension for exporting and sharing X posts with their source context.",
  "type": "module",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run format:check && npm run typecheck && npm test && npm run build",
    "lint:extension": "web-ext lint --source-dir dist --warnings-as-errors --no-config-discovery",
    "package": "npm run check && npm run lint:extension && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "jszip": "3.10.1",
    "prettier": "^3.9.6",
    "typescript": "^5.8.0",
    "vitest": "^3.2.4",
    "web-ext": "10.6.0"
  }
}
